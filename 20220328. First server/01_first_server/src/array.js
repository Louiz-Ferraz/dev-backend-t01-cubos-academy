const array = [1, 2, 4, 4, 6, 7, 1];

const arrayLetters = ['a', 'a', 'c', 'f'];

module.exports = {
    arrayNumbers: array,
    arrayLetters //não é necessário passar o nome da variável, se o nome da propriedade do objeto é igual
};